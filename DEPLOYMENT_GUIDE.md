# 🚀 Инструкция по деплою Dr.Protein на Railway с кастомным доменом

## Шаг 1: Создание аккаунта Railway (2 минуты)

1. Откройте: https://railway.app
2. Нажмите **"Login"** в правом верхнем углу
3. Выберите **"Login with GitHub"**
4. Авторизуйте Railway (доступ к репозиториям)

✅ **Готово!** У вас есть $5 бесплатных кредитов в месяц

---

## Шаг 2: Деплой проекта (3 минуты)

### 2.1 Создание проекта

1. На главной странице Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий: **`qrvoteio/drprotein-`**
4. Railway автоматически определит Node.js проект

### 2.2 Настройка переменных окружения

1. В открывшемся проекте перейдите на вкладку **"Variables"**
2. Нажмите **"+ New Variable"**
3. Добавьте переменную:
   - **Name:** `SESSION_SECRET`
   - **Value:** Любая случайная строка (например: `dr-protein-secret-key-2024-secure-random-string`)
4. Нажмите **"Add"**

> 💡 **Совет:** Для генерации безопасного ключа можете использовать: https://randomkeygen.com

### 2.3 Проверка настроек сборки

Railway автоматически определил команды, но проверьте:

1. Перейдите в **"Settings"** → **"Build"**
2. Убедитесь что указано:
   - **Build Command:** `npm run build`
   - **Start Command:** `npm start`
   - **Watch Paths:** `/**` (автоматически деплоится при изменениях)

✅ **Готово!** Railway начнет деплой автоматически

---

## Шаг 3: Получение временного домена (1 минута)

1. В проекте перейдите в **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"**
3. Railway создаст домен вида: `drprotein-production.up.railway.app`
4. Нажмите на домен чтобы открыть сайт

🎉 **Сайт работает!** Проверьте все страницы

---

## Шаг 4: Подключение вашего домена (5-10 минут)

### 4.1 Добавление домена в Railway

1. В **"Settings"** → **"Networking"** нажмите **"Custom Domain"**
2. Введите ваш домен (например: `drprotein.ru` или `www.drprotein.ru`)
3. Нажмите **"Add Domain"**

Railway покажет DNS записи которые нужно добавить:

```
Type: CNAME
Name: @ (для корневого домена) или www
Value: drprotein-production.up.railway.app
TTL: Auto
```

### 4.2 Настройка DNS у регистратора домена

#### Если ваш домен на **REG.RU:**

1. Войдите в личный кабинет REG.RU
2. Раздел **"Домены"** → выберите ваш домен
3. **"Управление зоной"** → **"Добавить запись"**
4. Настройте CNAME:
   - **Субдомен:** `@` (или `www`)
   - **Тип записи:** `CNAME`
   - **Значение:** `drprotein-production.up.railway.app`
5. Сохраните изменения

#### Если ваш домен на **Cloudflare:**

1. Войдите в Cloudflare
2. Выберите ваш домен
3. **DNS** → **"Add record"**
4. Настройте:
   - **Type:** `CNAME`
   - **Name:** `@` или `www`
   - **Target:** `drprotein-production.up.railway.app`
   - **Proxy status:** 🟠 **DNS only** (важно!)
5. Save

#### Если ваш домен на **Timeweb/Beget/другом хостинге:**

1. Найдите раздел **"Управление DNS"**
2. Добавьте CNAME запись:
   - Имя: `@` или `www`
   - Тип: `CNAME`
   - Значение: `drprotein-production.up.railway.app`
3. Сохраните

### 4.3 Проверка домена

1. Подождите 5-60 минут (DNS может обновляться медленно)
2. Откройте ваш домен в браузере
3. Railway автоматически выдаст SSL сертификат (HTTPS)

✅ **Готово!** Сайт доступен на вашем домене с HTTPS!

---

## Шаг 5: Автоматический деплой (настроен по умолчанию! ✅)

**Уже работает!** Каждый раз когда мы:

1. Вносим изменения в Replit
2. Отправляем код в GitHub (я это делаю)
3. **Railway автоматически деплоит новую версию!**

⏱️ **Время деплоя:** ~2-3 минуты после push в GitHub

### Как это выглядит:

```
Вы: "Измени цвет кнопки"
   ↓
Я вношу изменения → Тестирую → Отправляю в GitHub
   ↓
Railway видит изменения → Автоматически собирает → Деплоит
   ↓
Ваш домен обновлен! (через 2-3 минуты)
```

---

## 📊 Мониторинг и логи

### Просмотр логов:

1. В Railway проекте откройте вкладку **"Deployments"**
2. Выберите текущий деплой
3. Нажмите **"View Logs"**
4. Видите все логи сервера в реальном времени

### Проверка использования:

1. Вкладка **"Metrics"**
2. Смотрите:
   - CPU usage (загрузка процессора)
   - Memory (использование памяти)
   - Network (трафик)

💰 **Бюджет:** $5/месяц хватит на ~500 часов работы вашего сайта

---

## 🆘 Что делать если что-то пошло не так

### Проблема: Сайт не открывается после деплоя

1. Проверьте логи в Railway (см. выше)
2. Убедитесь что добавлена переменная `SESSION_SECRET`
3. Проверьте что в package.json есть скрипты `build` и `start`

### Проблема: Домен не подключается

1. Подождите 1-2 часа (DNS обновляется медленно)
2. Проверьте CNAME запись через: https://dnschecker.org
3. Убедитесь что в Cloudflare выключен оранжевый proxy (только DNS!)

### Проблема: "Out of credits"

1. Railway дает $5 бесплатно каждый месяц
2. Если превысили лимит, можно:
   - Подключить карту (списание только сверх $5)
   - Или подождать до следующего месяца

---

## ✅ Чеклист успешного деплоя

- [ ] Создан аккаунт Railway через GitHub
- [ ] Проект подключен из репозитория `qrvoteio/drprotein-`
- [ ] Добавлена переменная окружения `SESSION_SECRET`
- [ ] Railway сгенерировал временный домен (работает)
- [ ] CNAME запись добавлена у регистратора домена
- [ ] Ваш домен открывается и показывает сайт с HTTPS ✅
- [ ] Автодеплой работает (при изменениях в GitHub)

---

## 🎯 Следующие шаги

После успешного деплоя мы можем:

1. ✅ Продолжить разработку здесь в Replit
2. ✅ Каждое обновление автоматически попадет на ваш домен
3. ✅ Добавить Google Analytics/Yandex Metrika
4. ✅ Подключить базу данных PostgreSQL (когда понадобится)
5. ✅ Добавить email рассылку через SendPulse

---

## 📞 Нужна помощь?

Если возникнут проблемы на любом этапе - просто напишите мне:
- "Не получается добавить домен"
- "Ошибка в логах Railway"
- "Как обновить сайт"

Я помогу разобраться! 🚀
